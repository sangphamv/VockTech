---
import Container from "@/components/container.astro";
import Postlist from "@/components/postlist.astro";
import Layout from "@/layouts/Layout.astro";
import { latestPosts } from "@/utils/content";
import { SITE_CONFIG } from "@/site.config";
import Pagination from "@/components/Pagination.astro";

import type { PaginateFunction } from "astro";

export async function getStaticPaths({ paginate }: { paginate: PaginateFunction }) {
  return paginate(latestPosts, { pageSize: 8 });
}

const { page } = Astro.props;
---

<Layout title="">
  <Container>
    <main id="main-content">
      {page.currentPage === 1 && (
        <div class="min-h-[85vh] flex flex-col items-center justify-center text-center mb-24 md:mb-32">
          <h1 class="text-4xl md:text-6xl font-extrabold tracking-tight text-light-foreground dark:text-dark-foreground mb-6">
            Khám phá <span class="text-transparent bg-clip-text bg-gradient-to-r from-light-accent to-light-accent/70 dark:from-dark-accent dark:to-dark-accent/70">Thủ Thuật Công Nghệ</span> Đỉnh Cao
          </h1>
          <p class="text-lg md:text-xl text-light-foreground/70 dark:text-dark-foreground/70 max-w-2xl mb-10">
            Nơi chia sẻ kiến thức, kinh nghiệm và những xu hướng mới nhất về công nghệ, lập trình và phần mềm.
          </p>
          <div class="flex flex-col sm:flex-row gap-4">
            <a href="#latest-posts" class="w-full sm:w-auto px-8 py-3.5 bg-light-accent dark:bg-dark-accent text-light-foreground dark:text-dark-background font-bold rounded-xl hover:bg-light-accent/90 dark:hover:bg-dark-accent/90 transition-all shadow-sm hover:shadow-lg hover:-translate-y-1 duration-300 text-center">Đọc bài mới nhất</a>
            <a href="/danh-muc" class="w-full sm:w-auto px-8 py-3.5 border-2 border-light-surface dark:border-dark-surface text-light-foreground dark:text-dark-foreground font-medium rounded-xl hover:border-light-accent dark:hover:border-dark-accent hover:text-light-accent dark:hover:text-dark-accent transition-all duration-300 text-center">Khám phá danh mục</a>
          </div>
        </div>
      )}
      {page.currentPage > 1 && (
        <h1 class="sr-only">Trang {page.currentPage} - Trang chủ {SITE_CONFIG.siteName}</h1>
      )}
      
      <>
        <div class="mb-8 flex items-center justify-between">
          <h2 class="text-2xl md:text-3xl font-bold tracking-tight text-light-foreground dark:text-dark-foreground">
            <span class="relative inline-block">
              {page.currentPage === 1 ? "Bài viết nổi bật" : `Tất cả bài viết - Trang ${page.currentPage}`}
              <span class="absolute -bottom-2 left-0 w-1/2 h-1.5 bg-light-accent dark:bg-dark-accent rounded-full"></span>
            </span>
          </h2>
        </div>

        <div id="latest-posts" class="grid gap-10 lg:gap-10 md:grid-cols-2 scroll-mt-24">
          {
            page.data
              .slice(0, 2)
              .map((post: any) => (
                <Postlist post={post} aspect="landscape" preloadImage={page.currentPage > 1} />
              ))
          }
        </div>

        {page.data.length > 2 && (
          <>
            {page.currentPage === 1 && (
              <div class="mt-16 mb-8 flex items-center justify-between">
                <h2 class="text-2xl md:text-3xl font-bold tracking-tight text-light-foreground dark:text-dark-foreground">
                  <span class="relative inline-block">
                    Mới cập nhật
                    <span class="absolute -bottom-2 left-0 w-1/2 h-1.5 bg-light-accent dark:bg-dark-accent rounded-full"></span>
                  </span>
                </h2>
              </div>
            )}
            <div class={page.currentPage === 1 ? "grid gap-10 lg:gap-10 md:grid-cols-2 xl:grid-cols-3" : "grid gap-10 mt-10 lg:gap-10 md:grid-cols-2 xl:grid-cols-3"}>
              {
                page.data
                  .slice(2)
                  .map((post: any) => <Postlist post={post} aspect="square" preloadImage={false} />)
              }
            </div>
          </>
        )}
      </>

      <Pagination page={page} />
    </main>
  </Container>
</Layout>
