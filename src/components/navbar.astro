---
import Container from "@/components/container.astro";
import logoImage from "@/assets/logo.svg";
import { Image } from "astro:assets";
import { Icon } from "astro-icon/components";
import Themeswitch from "@/components/themeswitch.astro";
import { SITE_CONFIG } from "@/site.config";

interface Menu {
  label: string;
  href: string;
  external?: boolean;
  badge?: string;
}
const menuItems: Menu[] = [
  {
    label: "Trang chủ",
    href: "/",
  },
  {
    label: "Giới thiệu",
    href: "/gioi-thieu",
  },
  {
    label: "Danh mục",
    href: "/danh-muc",
  },

  {
    label: "Liên hệ",
    href: "/lien-he",
  },
];
---

<header
  id="navbar"
  class="sticky top-0 z-50 w-full bg-light-background/80 dark:bg-dark-background/80 backdrop-blur-md transition-transform duration-300">
  <Container class="!py-3 lg:!py-4">
    <nav class="relative">
      <div class="flex items-center justify-between">
        <div class="flex items-center">
          <a class="w-28" href="/" aria-label="Home">
            <div 
              class="w-full h-10 bg-light-accent dark:bg-dark-accent transition-colors" 
              style={`mask: url(${logoImage.src}) no-repeat left center; -webkit-mask: url(${logoImage.src}) no-repeat left center; mask-size: contain; -webkit-mask-size: contain;`}
            ></div>
            <span class="sr-only">{SITE_CONFIG.siteName}</span>
          </a>
        </div>

        <div class="hidden md:flex items-center gap-8">
          {
            menuItems.map((item) => (
              <a
                href={item.href}
                class="text-sm font-medium text-light-foreground/70 dark:text-dark-foreground/70 hover:text-light-accent dark:hover:text-dark-accent transition-colors focus-visible:ring-2 focus-visible:ring-light-accent dark:focus-visible:ring-dark-accent focus-visible:outline-none rounded-sm"
                target={item.external ? "_blank" : ""}
                rel={item.external ? "noopener" : ""}>
                {item.label}
              </a>
            ))
          }
        </div>

        <div class="flex items-center gap-3">
          <Themeswitch />
          <a
            href="/search"
            aria-label="Search"
            class="p-2 text-light-foreground/70 rounded-md hover:text-light-accent dark:hover:text-dark-accent dark:text-dark-foreground/70 dark:hover:text-dark-accent focus-visible:ring-2 focus-visible:ring-light-accent dark:focus-visible:ring-dark-accent focus-visible:outline-none">
            <Icon name="heroicons:magnifying-glass" class="w-6 h-6" />
          </a>

          <button
            id="menu"
            aria-label="Toggle Menu"
            class="md:hidden px-2 py-1 text-light-foreground/70 rounded-md focus:text-light-accent dark:text-dark-foreground/70 focus-visible:ring-2 focus-visible:ring-light-accent dark:focus-visible:ring-dark-accent focus-visible:outline-none">
            <svg
              class="w-6 h-6 fill-current"
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24">
              <path
                class="navmenu-toggle"
                fill-rule="evenodd"
                d="M4 5h16a1 1 0 0 1 0 2H4a1 1 0 1 1 0-2zm0 6h16a1 1 0 0 1 0 2H4a1 1 0 0 1 0-2zm0 6h16a1 1 0 0 1 0 2H4a1 1 0 0 1 0-2z"
              ></path>

              <path
                class="navmenu-toggle hidden"
                fill-rule="evenodd"
                clip-rule="evenodd"
                d="M18.278 16.864a1 1 0 0 1-1.414 1.414l-4.829-4.828-4.828 4.828a1 1 0 0 1-1.414-1.414l4.828-4.829-4.828-4.828a1 1 0 0 1 1.414-1.414l4.829 4.828 4.828-4.828a1 1 0 1 1 1.414 1.414l-4.828 4.829 4.828 4.828z"
              ></path>
            </svg>
          </button>
        </div>
      </div>

      <div id="mobile-menu" class="grid grid-rows-[0fr] transition-[grid-template-rows] duration-300 ease-in-out md:hidden">
        <div class="overflow-hidden">
          <div
            class="flex flex-col items-center justify-start mt-4 w-full border-t border-light-surface dark:border-dark-surface pt-2">
            {
              menuItems.map((item) => (
                <a
                  href={item.href}
                  class="w-full text-center py-2.5 text-sm font-medium text-light-foreground/70 dark:text-dark-foreground/70 hover:text-light-accent dark:hover:text-dark-accent"
                  target={item.external ? "_blank" : ""}
                  rel={item.external ? "noopener" : ""}>
                  {item.label}
                </a>
              ))
            }
          </div>
        </div>
      </div>
    </nav>
  </Container>
</header>

<script is:inline>
  const menuButton = document.getElementById("menu");
  menuButton.addEventListener("click", () => {
    [...document.querySelectorAll(".navmenu-toggle")].forEach((element) => {
      element.classList.toggle("hidden");
    });
    
    const mobileMenu = document.getElementById("mobile-menu");
    if (mobileMenu) {
      if (mobileMenu.classList.contains("grid-rows-[0fr]")) {
        mobileMenu.classList.remove("grid-rows-[0fr]");
        mobileMenu.classList.add("grid-rows-[1fr]");
      } else {
        mobileMenu.classList.remove("grid-rows-[1fr]");
        mobileMenu.classList.add("grid-rows-[0fr]");
      }
    }
  });

  let lastScrollTop = 0;
  const navbar = document.getElementById("navbar");

  window.addEventListener("scroll", () => {
    const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
    if (scrollTop > lastScrollTop && scrollTop > 50) {
      // Scroll Down -> Hide
      navbar.classList.add("-translate-y-full");
    } else {
      // Scroll Up -> Show
      navbar.classList.remove("-translate-y-full");
    }
    lastScrollTop = scrollTop <= 0 ? 0 : scrollTop;
  });
</script>
